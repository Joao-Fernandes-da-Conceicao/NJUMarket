# PostgreSQL 日期精度和时区修复说明

## 问题描述

**问题：**
1. PostgreSQL 存储的日期过于精确：`2025-11-19 19:22:37.953111+08`（微秒级精度）
2. 只需要秒级精确度：`2025-10-29 14:34:10+08`
3. PostgreSQL 的日期是带时区的（`timestamp with time zone`），需要正确处理时区转换

## 解决方案

### 1. 创建自定义日期转换器

创建了 `LocalDateTimeSecondsConverter` 类，实现 `AttributeConverter<LocalDateTime, Timestamp>`：

**功能：**
- **写入数据库时**：
  1. 将 `LocalDateTime` 截断到秒级精度（去除纳秒部分）
  2. 转换为 UTC 时区的 `Instant`（PostgreSQL `timestamp with time zone` 内部存储为 UTC）
  3. 创建 `Timestamp` 对象

- **从数据库读取时**：
  1. `Timestamp` 内部存储为 UTC 时间
  2. 转换为系统时区的 `LocalDateTime`
  3. 截断到秒级精度（确保没有纳秒部分）

### 2. 应用到 Commodity 实体

在 `Commodity` 实体的 `publishTime` 字段上应用转换器：

```java
@CreationTimestamp
@Column(name = "publish_time", nullable = false, columnDefinition = "timestamp with time zone")
@jakarta.persistence.Convert(converter = com.njumarket.commodity.config.LocalDateTimeSecondsConverter.class)
private LocalDateTime publishTime;
```

### 3. 更新 ES 同步逻辑

在 `CommoditySearchDocument.fromCommodity()` 方法中，确保同步到 ES 时也截断到秒级：

```java
// 确保 publishTime 截断到秒级精度（去除纳秒部分）
if (commodity.getPublishTime() != null) {
    document.setPublishTime(commodity.getPublishTime().truncatedTo(java.time.temporal.ChronoUnit.SECONDS));
} else {
    document.setPublishTime(null);
}
```

### 4. 增强 ES 日期转换器

更新 `ElasticsearchConversionConfig` 中的转换器，支持解析 PostgreSQL 格式的日期：

- 支持格式：`2025-11-19 19:22:37.953111+08`（带微秒和时区）
- 自动移除微秒部分，只保留秒级精度
- 正确处理时区转换

## 修改的文件

1. **LocalDateTimeSecondsConverter.java**（新建）
   - 自定义日期转换器，处理秒级精度和时区转换

2. **Commodity.java**
   - 在 `publishTime` 字段上应用转换器

3. **CommoditySearchDocument.java**
   - 在同步到 ES 时截断日期到秒级精度

4. **ElasticsearchConversionConfig.java**
   - 增强日期转换器，支持 PostgreSQL 格式的日期解析

## 时区处理说明

### PostgreSQL 时区存储机制

- PostgreSQL 的 `timestamp with time zone` 类型：
  - **内部存储**：始终以 UTC 时间存储
  - **显示格式**：根据客户端时区设置显示
  - **示例**：`2025-11-19 19:22:37+08` 在数据库中存储为 UTC 时间 `2025-11-19 11:22:37+00`

### 转换器时区处理

1. **写入时**：
   - `LocalDateTime`（系统时区，如 UTC+8）→ UTC `Instant` → PostgreSQL UTC 存储

2. **读取时**：
   - PostgreSQL UTC 存储 → UTC `Instant` → `LocalDateTime`（系统时区，如 UTC+8）

3. **ES 同步时**：
   - 从数据库读取的 `LocalDateTime` 已经是系统时区，直接截断到秒级即可

## 测试建议

1. **数据库写入测试**：
   - 创建新商品，检查 `publish_time` 字段是否为秒级精度
   - 验证时区转换是否正确

2. **数据库读取测试**：
   - 从数据库读取商品，检查 `publishTime` 是否为秒级精度
   - 验证时区是否正确转换为系统时区

3. **ES 同步测试**：
   - 同步商品到 ES，检查 `publishTime` 是否为秒级精度
   - 验证 ES 中的日期格式是否正确

4. **ES 读取测试**：
   - 从 ES 读取商品，验证日期转换是否正确

## 注意事项

1. **系统时区设置**：
   - 确保应用服务器的时区设置正确（建议设置为 `Asia/Shanghai` 或 `UTC+8`）

2. **数据库时区设置**：
   - PostgreSQL 的时区设置不影响 `timestamp with time zone` 的存储（始终为 UTC）
   - 但会影响显示格式

3. **兼容性**：
   - 旧数据可能仍包含微秒精度，读取时会自动截断到秒级
   - 新数据写入时会自动截断到秒级

4. **性能影响**：
   - 转换器在每次读写时都会执行，但性能影响很小（只是简单的截断和时区转换）

