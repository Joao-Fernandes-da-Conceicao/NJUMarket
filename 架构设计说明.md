# Elasticsearch + 数据库混合架构设计说明

## 当前架构设计

### 1. 搜索流程

```
用户搜索请求
    ↓
CommodityQueryService.search()
    ↓
CommoditySearchService.search()  ← 只返回商品ID列表
    ↓
从数据库根据ID列表查询完整商品信息
    ↓
返回给用户
```

### 2. 为什么只从 ES 获取 ID，不从 ES 获取完整数据？

**这是业界标准做法，原因如下：**

#### ✅ 优点：

1. **数据一致性**
   - ES 是搜索索引，不是数据源
   - 数据库是唯一真实数据源（Single Source of Truth）
   - 避免 ES 和数据库数据不一致的问题

2. **数据完整性**
   - ES 只存储搜索需要的字段（title, description, category 等）
   - 完整商品信息（库存、价格、卖家信息等）存储在数据库
   - 避免在 ES 中存储大量冗余数据

3. **实时性**
   - 价格、库存等关键信息需要实时查询数据库
   - ES 同步可能有延迟，不适合实时数据

4. **灵活性**
   - 可以随时修改数据库字段，不影响 ES 索引结构
   - ES 只负责搜索，数据库负责业务逻辑

5. **性能优化**
   - ES 只返回 ID（轻量级），减少网络传输
   - 数据库查询可以使用批量查询（IN 查询）
   - 可以添加缓存层进一步优化

#### 📚 业界实践：

- **Elasticsearch 官方建议**：ES 作为搜索索引，数据库作为数据源
- **电商平台**（如淘宝、京东）：搜索返回商品ID，详情从数据库/缓存获取
- **内容平台**（如知乎、微博）：搜索返回内容ID，完整内容从数据库获取

### 3. 是否需要 Spring Cache？

**建议添加 Spring Cache，原因：**

#### ✅ 添加缓存的好处：

1. **减少数据库查询**
   - 热门商品会被频繁查询
   - 缓存可以显著减少数据库压力

2. **提升响应速度**
   - 缓存命中时响应时间 < 10ms
   - 数据库查询通常需要 50-200ms

3. **降低系统负载**
   - 减少数据库连接数
   - 减少数据库 CPU 和 IO 压力

#### 🎯 缓存策略建议：

```java
// 1. 商品详情缓存（单个商品）
@Cacheable(value = "commodity", key = "#commodityId")
public CommodityDTO getCommodityById(String commodityId) {
    // 从数据库查询
}

// 2. 商品列表缓存（批量查询）
@Cacheable(value = "commodities", key = "#ids.toString()")
public List<CommodityDTO> getCommoditiesByIds(List<String> ids) {
    // 从数据库批量查询
}

// 3. 缓存更新（商品更新时）
@CacheEvict(value = {"commodity", "commodities"}, allEntries = true)
public void updateCommodity(Commodity commodity) {
    // 更新数据库
    // 同步到 ES
}
```

#### 📊 缓存配置建议：

```yaml
spring:
  cache:
    type: redis  # 或 caffeine（本地缓存）
    redis:
      time-to-live: 3600000  # 1小时过期
      cache-null-values: false  # 不缓存null值
    caffeine:
      spec: maximumSize=10000,expireAfterWrite=1h
```

### 4. 完整架构流程（推荐）

```
用户搜索请求
    ↓
ES 搜索（返回商品ID列表 + 高亮）
    ↓
检查缓存（Redis/Caffeine）
    ├─ 缓存命中 → 直接返回
    └─ 缓存未命中
        ↓
    数据库批量查询（IN 查询）
        ↓
    写入缓存
        ↓
    返回给用户
```

### 5. 性能对比

| 方案 | 响应时间 | 数据库压力 | 数据一致性 | 复杂度 |
|------|---------|-----------|-----------|--------|
| ES 返回完整数据 | 50-100ms | 低 | 可能不一致 | 低 |
| ES 返回ID + 数据库 | 100-200ms | 中 | 一致 | 中 |
| ES 返回ID + 数据库 + 缓存 | 10-50ms | 低 | 一致 | 中高 |

### 6. 总结

1. **当前架构（ES 返回 ID + 数据库查询）是标准做法**，不是权宜之计
2. **建议添加 Spring Cache**，进一步提升性能
3. **ES 只负责搜索**，数据库负责数据存储，缓存负责性能优化

### 7. 实施建议

1. **第一阶段**：保持当前架构，修复日期转换问题 ✅
2. **第二阶段**：添加 Spring Cache（Redis 或 Caffeine）
3. **第三阶段**：优化缓存策略（预热、异步刷新等）

